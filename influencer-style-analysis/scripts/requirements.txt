# Core dependencies
anthropic>=0.40.0
openai>=1.50.0
httpx>=0.27.0
pydantic>=2.0.0
pydantic-settings>=2.0.0

# Audio processing (optional but recommended)
# Requires system ffmpeg installation
pydub>=0.25.1

# Audio transcription (optional fallback)
faster-whisper>=1.0.0
