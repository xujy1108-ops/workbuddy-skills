# macOS
.DS_Store

# Node
node_modules/
package-lock.json

# 打包产物
*.zip

# 临时文件
*.log
*.tmp
*.bak

# 环境变量（含密钥）
.env

# Python
__pycache__/
*.pyc

# 测试产物
analysis_result*.json

# 热点入表失败时的落盘文件（用于重放，不入库）
pending_bitable_*.json

# lark-cli 读表暂存产物（ndjson 明细 + manifest 清单）
*.ndjson
*.manifest.json
